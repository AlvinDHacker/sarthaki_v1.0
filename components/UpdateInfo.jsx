import React from "react";

const UpdateInfo = () => {
  return (
    <div>
      {data.map((values, index) => (
        <div>
          <h1>Hello</h1>
        </div>
      ))}
    </div>
  );
};

export default UpdateInfo;
